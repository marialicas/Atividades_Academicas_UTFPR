//imprime os valores de 0 a 10
// quando quer fazer o teste no inicio, quando o numero de vez nao eh conhecido
#include<stdio.h>

int main ()
{
    int i=0;
    while(i<=10)
    {
        printf("%d\t",i);
        i++;
    }
}
